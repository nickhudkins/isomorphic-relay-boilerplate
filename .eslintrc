{
  "env": {
    "node": true
  },
  "extends": "airbnb",
  "parser": "babel-eslint",
  "rules": {
    // TODO: remove that line when eslint-plugin-react is fixed (now it produces false positives)
    "react/prefer-stateless-function": 1
  }
}
